# تعليمات النشر على Vercel

## الطريقة 1: النشر مباشرة من Vercel Dashboard (الأسهل)

### الخطوات:

1. **إنشاء حساب على Vercel** (إذا لم يكن لديك):
   - اذهب إلى: https://vercel.com
   - سجل دخول باستخدام GitHub/GitLab/Bitbucket

2. **رفع المشروع إلى GitHub**:
   ```bash
   # في مجلد المشروع
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin YOUR_GITHUB_REPO_URL
   git push -u origin main
   ```

3. **النشر على Vercel**:
   - اذهب إلى: https://vercel.com/new
   - اضغط على "Import Project"
   - اختر المستودع الخاص بك
   - Vercel سيكتشف تلقائياً أنه Next.js project
   - اضغط على "Deploy"

4. **الإعدادات**:
   - Framework Preset: Next.js (يتم اكتشافه تلقائياً)
   - Root Directory: `./` (افتراضي)
   - Build Command: `npm run build` (افتراضي)
   - Output Directory: `.next` (افتراضي)
   - Install Command: `npm install` (افتراضي)

5. **بعد النشر**:
   - ستحصل على رابط مثل: `https://your-project.vercel.app`
   - يمكنك تخصيص اسم النطاق من Settings > Domains

---

## الطريقة 2: النشر باستخدام Vercel CLI

### الخطوات:

1. **تثبيت Vercel CLI**:
   ```bash
   npm install -g vercel
   ```

2. **تسجيل الدخول**:
   ```bash
   vercel login
   ```

3. **النشر**:
   ```bash
   cd demo-islamic-library
   vercel
   ```

4. **للمتابعة**:
   - اضغط Enter للمتابعة
   - اختر المشروع أو أنشئ مشروع جديد
   - Vercel سيبدأ عملية النشر تلقائياً

5. **للإنتاج**:
   ```bash
   vercel --prod
   ```

---

## ملاحظات مهمة:

### ✅ المشروع جاهز للنشر:
- ✅ Build يعمل بنجاح
- ✅ جميع الصور في `/public/images` ستعمل تلقائياً
- ✅ Next.js config محسّن
- ✅ TypeScript config صحيح

### 📝 تأكد من:
1. جميع الصور موجودة في `/public/images`
2. جميع الكتب في `/data/books.ts` تحتوي على صور صحيحة
3. رقم WhatsApp في `page.tsx` هو الرقم الصحيح

### 🔧 بعد النشر:
- يمكنك تغيير رقم WhatsApp من Vercel Environment Variables
- يمكنك تحديث الكتب من GitHub وستحدث تلقائياً
- جميع التحديثات على `main` branch ستُنشر تلقائياً

---

## استكشاف الأخطاء:

### إذا فشل Build:
- تحقق من Console في Vercel Dashboard
- تأكد من أن جميع الصور موجودة
- تأكد من أن TypeScript لا يوجد به أخطاء

### إذا لم تظهر الصور:
- تأكد من أن جميع الصور في `/public/images`
- تحقق من المسارات في `/data/books.ts`

### إذا لم يظهر الموقع:
- تحقق من Build Logs في Vercel
- تأكد من أن Build Command نجح

---

## تحديث المشروع:

بعد أي تعديلات:
```bash
git add .
git commit -m "Update project"
git push
```

Vercel سينشر التحديثات تلقائياً! 🚀



